name: Release

on:
  push:
    branches:
      - 'release'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci:
    uses: LastDragon-ru/lara-asp/.github/workflows/ci.yml@master

  release:
    name: Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    needs:
      - ci
    steps:
      # Bot cannot sign tags/commits yet
      # https://github.com/actions/runner/issues/667
      - name: Import GPG key
        id: gpg
        uses: crazy-max/ghaction-import-gpg@v5
        with:
          gpg_private_key: ${{ secrets.GPG_PRIVATE_KEY }}
          passphrase: ${{ secrets.GPG_PASSPHRASE }}
          git_config_global: true
          git_user_signingkey: true
          git_commit_gpgsign: true
          git_tag_gpgsign: true

      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Install Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16
          cache: npm

      - name: Install dependencies
        run: |
          npm ci

      - name: Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          npm run release

      - name: Congratulations
        run: |
          VERSION=$(git describe --tags --abbrev=0)

          echo "Version **[${VERSION}](https://github.com/${{ github.repository }}/releases/tag/${VERSION})** released! :rocket:" >> $GITHUB_STEP_SUMMARY

  packages:
    uses: LastDragon-ru/lara-asp/.github/workflows/packages.yml@master
    needs:
      - release
    secrets: inherit
