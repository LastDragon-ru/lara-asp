type Query {
    a(order: A @sortBy): ID! @all
}

input A {
    "Description should be ignored."
    id: ID!
    name: String!
    child: B!
}

input B {
    "Description should be ignored."
    id: ID!
    name: String
    parent: A!
}
