type Query {
    a(order: _ @sortBy): [A!]! @all
}

type A {
    "Description should be copied."
    id: ID!
    name: String!
    child: B!
    children: [B!]!
    argument(arg: String): A!
    arguments(arg: String): [A!]
}

type B {
    "Description should be copied."
    id: ID!
    name: String
    parent: A!
    argument(arg: String): B!
    arguments(arg: String): [B!]
}
